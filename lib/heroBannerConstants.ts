/**
 * Fixed hero banner size for consistent display on all screen sizes.
 * Portal crop forces this aspect ratio; frontend uses same ratio so full banner always shows.
 */
export const HERO_BANNER_ASPECT = 3 / 1; // width / height (e.g. 1920×640)
export const HERO_BANNER_WIDTH = 1920;
export const HERO_BANNER_HEIGHT = 640; // 1920 / 3

export const HERO_BANNER_SIZE_LABEL = '1920×640 px (3:1)';
